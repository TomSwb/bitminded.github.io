/**
 * User Access Management Translations
 * Handles translations for the user access management component
 */

if (typeof window.userAccessManagementTranslations === 'undefined') {
    window.userAccessManagementTranslations = {
        init: function() {
            // Translations are loaded via JSON file in the component
            // This file is kept for consistency with other components
            console.log('✅ User Access Management translations ready');
        }
    };
}

